%section#records
  = render 'records'
%aside#actions
  = render 'actions'
= render '/refinery/admin/make_sortable', :tree => false if !searching? and ::Refinery::Midas::Admin::AccountsController.sortable? and ::Refinery::Midas::Account.count > 1
