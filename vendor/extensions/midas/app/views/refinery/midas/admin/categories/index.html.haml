%section#records
  = render 'records'
%aside#actions
  = render 'actions'
= render '/refinery/admin/make_sortable', :tree => false if !searching? and ::Refinery::Midas::Admin::CategoriesController.sortable? and ::Refinery::Midas::Category.count > 1
