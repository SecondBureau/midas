<div class="filter">
	<%= link_to 'New Payment mode', new_payment_mode_path, :class=>'button' %>
</div>

<table>
  <tr>
    <th>Label</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @payment_modes.each do |payment_mode| %>
  <tr>
    <td><%= payment_mode.label %></td>
    <td><%= link_to 'Show', payment_mode %></td>
    <td><%= link_to 'Edit', edit_payment_mode_path(payment_mode) %></td>
    <td><%= link_to 'Delete?', {:action=>:destroy, :id=>payment_mode.id}, :confirm=>"Are you sure", :method=>:delete %></td>
  </tr>
<% end %>
</table>